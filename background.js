 
 /* chrome.storage.onChanged.addListener(function (changes) {
    console.log(changes.heightArticles.newValue);
    console.log(changes.heightArticles.oldValue);

    chrome.scripting.executeScript({
      target: { tabId: tab.id },
      function: click
    });
  });
  

  function click () {
    window.scroll(0, 1000);
  }*/
 /*function scroling (height = 1000) {
    window.scroll(0, scrolTo + height);
    chrome.storage.local.set({"scrolTo": scrolTo + height}, function() {
      console.log('scrolTo is set to ' + (scrolTo + height));
    });  
  }  */



//------------------------



//let currentScrol = window.scrollY;
/*
chrome.runtime.onInstalled.addListener(() => {
  chrome.storage.local.set({ "currentScrol": window.scrollY })  
});*/
